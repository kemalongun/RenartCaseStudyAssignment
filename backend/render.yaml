services:
  - type: web
    name: renart-api
    env: node
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: PORT
        value: 8080
      - key: GOLD_API_KEY
        value: goldapi-6acwsmg8d4u83-io